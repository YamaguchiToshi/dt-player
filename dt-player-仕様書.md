# dt-player 実装仕様書

タップ演奏データの演奏データを、画面タップまたはスイッチ入力で1音ずつ進めて演奏する Web アプリ。GitHub Pages で配信する。

この文書は Claude Code への指示書として書いている。既存のプロトタイプ `プロトタイプ.html` が単一 HTML として動作しており、これを分割・拡張して公開用リポジトリにするのが作業の中身。

---

## 1. 背景と用途

連携先の iPad アプリ は開発元が開発する iPad 用のユニバーサル楽器アプリ。「メロディ」機能で JSON 形式の演奏データを読み込み、1回のタップで1イベントずつ進める。テンポと音の長さは操作者が決める。iPad の画面タップは外部スイッチインターフェースからも送れるため、重度の身体障害がある人が音楽表現をする手段になる。

dt-player はその周辺ツール。目的は2つ。

1. 連携先の iPad アプリ に読み込む前に、作った JSON を PC やタブレットのブラウザで試聴・検証する
2. 連携先の iPad アプリ がない環境でも、同じ操作体系で演奏を体験できるようにする

利用者は支援者(教員・療法士)と、スイッチ操作をする本人の両方。

---

## 2. 連携先の iPad アプリ のデータ形式

イベントの配列。1要素が1タップに対応する。

```json
[
  {"values": [72, 64, 48], "velocity": 100},
  {"values": [74], "velocity": 100}
]
```

| キー | 内容 |
|---|---|
| `values` | 同時に鳴らす MIDI ノート番号の配列。1〜3要素 |
| `velocity` | 0〜127 の整数。公式サンプルは全て 100 |

**この形式には時間情報が一切ない。** 発音タイミング、音の長さ、ノートオフ、テンポ、休符は持たない。それらは全て操作者の入力が決める。これが仕様であり、欠落ではない。dt-player もこの前提を崩してはいけない。

### 制約

- **音域は MIDI 48〜84(C3〜C6)。** iPad の 連携先の iPad アプリ 実機で検証した結果、この範囲外は発音しない。dt-player が読み込む JSON もこの範囲を前提にするが、範囲外の音が来た場合は落とさず、鳴らさないだけにして警告を出す
- `values` の配列の順序には意味がない(`[58,66]` と `[66,58]` が公式サンプル内に混在する)。ただし本プロジェクトで生成するデータは第1要素を旋律にそろえている
- 同じ音の連続打鍵が頻出する(公式サンプルにもある)。ノートオフを送らずノートオンだけを送る実装だと2回目が鳴らないことがあるため、発音前に必ず前の音を止める
- ファイル名は NFC で正規化する(macOS 由来の NFD 濁点が混入すると照合が崩れる)

---

## 3. 現行プロトタイプの実装

`プロトタイプ.html` は単一ファイルで、以下を実装済み。移植時に失わないこと。

### 演奏ロジック

- スペースキー / Enter / 画面のポインタ押下で `press()`、離すと `release()`
- `press()` は「前の音を全て止める → 現在インデックスのイベントを発音 → インデックスを1進める」
- 押している間だけ音が伸びる。`release()` で減衰して止まる。ピアノ音色のみ押下後に自然減衰し、リリースを無視する
- キーの自動リピート(`ev.repeat`)は無視する。押し続けても進まない
- `ev.code` が空になる入力機器に備え、`ev.key` でも判定する(Bluetooth スイッチ対策)

### 音源(Web Audio API での合成)

3音色。`{osc, parts, a, d, r, cut, gain}` のテーブルで定義。

| 音色 | 波形 | 持続 |
|---|---|---|
| オルガン | sine + 倍音 | 押している間ずっと伸びる |
| ピアノ | triangle | 減衰する(d=1.8秒) |
| ストリングス | sawtooth を微妙にずらして重ねる | 押している間ずっと伸びる |

マスターゲインは 0.22。ノートごとに gain + lowpass を作り、`linearRampToValueAtTime` でアタック、減衰音色は `exponentialRampToValueAtTime` を使う。

### iPadOS Safari 対応(実機検証済み)

以下は実機で問題が出て直した箇所。理由を理解せずに削らないこと。

- **音声出力の解錠**: 最初のタップで AudioContext を生成し、長さ1サンプルの無音バッファを鳴らす。加えて `<audio>` 要素で無音 WAV を volume 0.001 でループ再生し、音声セッションを維持する(コントロールセンターで消音されていても鳴るようにするため)
- **canvas の高さ**: `height:100%` は Safari のグリッド行内で解決されず0になる。`position:relative` のラッパーを置き、canvas を `position:absolute` で広げ、サイズはラッパーの `getBoundingClientRect()` から取る。グリッド行は `minmax(0,1fr)`
- **サイズ取得のリトライ**: 測定値が 40px 未満なら `requestAnimationFrame` で測り直す
- **roundRect を使わない**: iPadOS 16.3 以前に未実装。例外が出ると描画ループごと止まる。`fillRect` で描く
- **描画ループの保護**: 描画処理全体を try/catch で囲み、例外時は画面にエラー文を出す(白画面になるのを防ぐ)
- **レイアウト**: `position:fixed; inset:0` と `100dvh`。`env(safe-area-inset-*)` でセーフエリアを確保。ブレークポイントは 834px
- **ジェスチャ抑制**: 演奏画面で `dblclick` / `contextmenu` / `gesturestart` を preventDefault。`touch-action:none`、`-webkit-touch-callout:none`、`-webkit-tap-highlight-color:transparent`
- **バックグラウンド**: `visibilitychange` で音を止める
- **`??` 演算子を使わない**(古い Safari 対策で置換済み)

### 画面

- 横スクロールするピアノロール。画面幅の28%の位置に固定した縦線が「いま」
- 左端30pxに鍵盤。Cの位置に音名を表示
- これから鳴る音は青灰色 `#7fa8b8`、鳴った音は `#3a4854`、いま鳴っている音は橙 `#f5a524`(グロー付き)
- 次に鳴る音の音名を画面下にテキストでも出す(色の判別が難しい人への手がかり)
- 「1つ戻す」「最初から」ボタン、`現在 / 全体` のカウンタ
- `prefers-reduced-motion` を尊重。有効時はスクロールを補間せず即座に切り替える

---

## 4. 収録データ

`dt-player` に埋め込み済みの7ファイル。全て著作権の保護期間が終了した楽曲。

| 曲名 | タップ数 | 音域 | 備考 |
|---|---|---|---|
| きらきら星 | 42 | 48-81 | ハ長調。旋律+伴奏2音 |
| トルコ行進曲 | 112 | 48-84 | モーツァルト K.331 第3楽章、主題24小節 |
| 木星 | 89 | 48-79 | ホルスト「Thaxted」。旋律+内声+低音 |
| 木星 オクターブ版 | 89 | 48-79 | 旋律をオクターブで重ねた厚い響き |
| ラデツキー行進曲 ショート版 | 98 | 49-78 | 序奏+主部のみ |
| ラデツキー行進曲 完全版 | 549 | 49-79 | 序奏・主部・中間部・トリオ・終止 |
| 荒野の果てに | 77 | 48-74 | GLORIA(仏キャロル)。1番+リフレイン |

### 出典と検証方法(README に記載すること)

いずれも旋律は公開されている楽譜データと1音ずつ照合済み。

- トルコ行進曲: piano-midi.de 由来の MIDI(`ClassicalPianoMIDI-dataset`)の右手最上声と照合
- 木星 / 荒野の果てに: Hymnary.org でパブリックドメインとして公開されている MusicXML。荒野の果てには四声譜と旋律譜の2種を突き合わせて確認
- ラデツキー行進曲: ニコニ・コモンズの MIDI 素材「【クラシック】ラデツキー行進曲」(作品番号 nc891、投稿者 nano_001、2008/08/16 登録)から抽出。第1ヴァイオリンを主軸に、フルート・クラリネットと音の一致を確認。トリオは第1ヴァイオリンに打ち込みの乱れがあったためオーボエを使用
  - 素材ページ: https://commons.nicovideo.jp/works/nc891
  - ライセンス画面の控え: `docs/license-nc891.png`(ログイン後の利用条件表を保存したもの)

  利用条件(ライセンス画面の全項目):

  | 項目 | 条件 |
  |---|---|
  | 利用許可範囲 | インターネット全般 |
  | ニコニコ投稿時の親子登録 | 任意 |
  | 権利表記 | 任意 |
  | 動画配信サイトでの収益化 | 個人:OK、法人:要連絡 |
  | 営利利用 | 個人:要連絡、法人:要連絡 |

  - 「インターネット全般」なので niconico 内に限定されない。GitHub Pages での公開は許諾範囲に入る
  - **権利表記は「任意」。** 出典明記は許諾上の義務ではない。本プロジェクトが README に書くとすれば、それは礼儀と追跡可能性のためであって、ライセンス要件ではない
  - 親子登録は niconico に投稿する場合の任意項目であり、本プロジェクトには課されない
  - **営利利用は個人でも「要連絡」。** 無償で配る限りは問題ないが、有償の教材・製品に組み込む、企業として配布するなどの段階になったら、事前に nano_001 氏へ連絡が必要になる。ここが将来の分岐点

**打ち込み MIDI 由来のものは、楽曲自体がパブリックドメインでも、打ち込んだ人の権利が及ぶ可能性がある。** 抽出したのは原曲の音そのものなので問題は薄いと考えるが、README に出典を明記する。

ラデツキー行進曲については上記のとおり出典と許諾範囲が特定できている。本プロジェクトが配布するのは MIDI ファイルそのものではなく、そこから抽出した音高データを タップ演奏データの JSON に書き直したもの(ニコニ・コモンズでいう子作品にあたる)。素材の再配布ではない点を README でも明確にする。

### タップ用の簡略化(共通の方針)

- 装飾音、トリルは省く
- 右手の重音は上声のみを旋律とする
- 伴奏は原曲の和声に沿って、低音(根音)+内声(和音構成音)の2音に絞る
- 1イベントは最大3音(公式サンプルの最大が3音のため)
- 旋律が低くて3音が入らない箇所は2音または1音にする。旋律は必ず鳴らす

---

## 5. これから作る構成

```
dt-player/
  index.html          UI の骨格
  css/style.css
  js/
    app.js            状態管理・入力・画面遷移
    player.js         演奏ロジック(press / release / step)
    synth.js          音源の抽象化(合成音 / SoundFont)
    roll.js           ピアノロールの描画
    loader.js         JSON の読み込みと検証
  data/
    songs.json        曲の一覧(メタ情報 + ファイル名)
    songs/*.json      個別の演奏データ
  sf/                 SoundFont(第3段階で追加)
  manifest.json       PWA(第3段階で追加)
  README.md
  LICENSE
```

曲データを外部ファイルにする理由は、曲を追加するときに HTML を触らずに済むこと、そして `data/songs/*.json` をそのまま 連携先の iPad アプリ 用にダウンロードできることの2つ。

### `data/songs.json` の形

```json
{
  "songs": [
    {
      "id": "kirakira-boshi",
      "title": "きらきら星",
      "file": "songs/kirakira-boshi.json",
      "taps": 42,
      "range": [48, 81],
      "note": "ハ長調。1タップで旋律と伴奏2音",
      "source": "伝承曲(パブリックドメイン)"
    }
  ]
}
```

`taps` と `range` は一覧表示用のキャッシュ。実データと食い違ったら実データを正とする。

---

## 6. 作業段階

一度に全部やると切り分けが難しくなる。段階ごとに iPad 実機で確認する。

### 第1段階: そのまま公開する

1. リポジトリ `dt-player` を作る
2. 現行の単一 HTML を `index.html` としてそのまま置く
3. GitHub Pages を有効化(main ブランチのルート)
4. `https://<user>.github.io/dt-player/` を iPad Safari で開き、音が鳴ること・ピアノロールが表示されることを確認

この段階で「URL で開ける」という価値がすでに出る。ここで止めても成立する。

### 第2段階: ファイルを分割し、曲データを外に出す

1. 上記の構成にファイルを分ける
2. `data/songs.json` を `fetch` で読み、一覧を作る
3. 曲を選んだら `data/songs/<id>.json` を `fetch` して読み込む。取得済みのものはメモリにキャッシュする
4. 読み込み失敗時のメッセージを出す(「曲データを読み込めませんでした」+ 再試行)
5. ローカルファイル読み込み(`<input type="file">`)は現行機能なので維持する
6. 各曲にダウンロードリンクを付ける(`<a download>` で `data/songs/*.json` を指す)

**注意**: `fetch` を使うと `file://` で開いたときに動かなくなる。単一 HTML 版を配布用に残したい場合は、`data/songs.json` の取得に失敗したら埋め込み定数にフォールバックする作りにする。不要なら割り切ってよい。

### 第3段階: 音質と PWA

#### SoundFont

WebAudioFont を使う。理由は、GM 音源が1楽器1 JS ファイル(数百KB)にまとまっており、**サンプルのループ情報を持っているため音を無限に伸ばせる**こと。押している間ずっと伸ばす必要があるこのアプリでは、ループ情報の有無が決定的。

- soundfont-player(MIDI.js 形式)は**採用しない**。サンプルがループせず、減衰して消えるだけなのでオルガンやストリングスの持続演奏ができない
- FluidSynth の WebAssembly 版(js-synthesizer)は音質が最良で SF2 をそのまま使えるが、wasm + SF2 で数MB〜数十MB。iPad での読み込み時間とメモリを考えると過剰

実装方針:

1. `synth.js` に共通インターフェースを作る: `init()` / `noteOn(midi, velocity)` / `releaseAll()` / `setTone(id)`
2. 合成音の実装(現行)を既定とし、SoundFont の実装を差し替え可能にする
3. 音源ファイルは `sf/` に置きリポジトリに含める。CDN に依存しない(外部が止まっても動くようにする)
4. **読み込みに失敗したら合成音に自動でフォールバックする。** 音が出ないより音質が落ちるほうが良い
5. 全音域のサンプルは不要。2〜3半音ごとに1つ用意し `playbackRate` でずらす
6. 楽器は2〜3種類に絞る(持続系1つ、減衰系1つが最低限)
7. **ライセンスを確認する。** FluidR3 や GeneralUser GS は再配布の条件が定められている。学校配布の可能性があるので、条件と帰属表示を `sf/LICENSE` と README に書く

#### PWA

- `manifest.json` にアイコン・名前・`display: standalone` を置く。iPad で「ホーム画面に追加」したときに全画面アプリとして起動し、演奏中に Safari のバーが出てこない
- Service Worker でファイルをキャッシュすれば2回目以降はオフラインで動く。学校の通信環境が不安定な場合に効く
- **Service Worker は最後に入れる。** 更新の反映が分かりにくくなり、開発中の切り分けが難しくなる

---

## 7. 第2段階以降で足したい機能

優先度順。すべて必須ではない。

1. **キーリピートの明示的な無効化の再確認** — スイッチインターフェースの機種によって挙動が違う。二重入力が起きたら、押下から一定時間(50ms程度)の再入力を無視するデバウンスを入れる
2. **音域チェック** — 読み込んだ JSON に 48〜84 の外の音があれば警告を出す。「連携先の iPad アプリ 実機では鳴りません」と伝える。データは落とさない
3. **セクション再生** — 開始位置と終了位置を指定して部分練習する。ラデツキー完全版(549タップ)では実質必須
4. **タップ数の表示位置** — 現在は下部のカウンタのみ。長い曲では進捗バーがあると見通しが良い
5. **音の長さデータ(任意)** — この形式には時間情報がないため、長い音と短い音の区別が画面に出せない。別ファイルで長さのヒントを持たせ、ロールの横幅に反映させることは可能。ただし 連携先の iPad アプリ 本体には読み込めない付加情報なので、あくまで練習補助として分けて扱う

---

## 8. 設計上の判断として守ること

このアプリの利用者にスイッチ操作をする人が含まれる。以下は機能ではなく前提として扱う。

- **押している間だけ音が伸びる挙動を変えない。** 長い音を「押し続けて待つ」ことがこのアプリの表現の核。タップで発音して自動で減衰する仕様にすると、表現の幅が失われる
- **色だけで情報を伝えない。** 次に鳴る音は必ずテキストでも示す
- **キーボード操作だけで全機能が使える状態を保つ。** フォーカス表示を消さない
- **押し間違いから復帰できるようにする。**「1つ戻す」は必須機能
- **`prefers-reduced-motion` を尊重する**
- **エラー時に無言で止まらない。** 音が出ない・データが読めない・描画できないのいずれも、画面に理由を出す。支援者がその場で判断できることが重要

---

## 9. リポジトリ名と URL

- リポジトリ名: `dt-player`(ハイフン区切り)
- 公開 URL: `https://<user>.github.io/dt-player/`

## 10. 公開時の確認事項

### 現時点の配布範囲(2026-09-13 時点の判断)

初回の配布先は、開発者が講師を務めた研修会の参加者。スイッチを使って肢体不自由のある子どもと遊ぶアクティビティの一つとして紹介するもので、**広く一般に広報する予定はない。** そのため README は当面作らない方針とした。

ラデツキー行進曲の素材は権利表記が「任意」なので、README がなくても許諾上の問題はない。出典の記録は本仕様書と `docs/license-nc891.png` が担う。

以下のチェックリストは、一般公開に踏み出す段階で改めて通す。

- [ ] 収録データが全て保護期間終了の楽曲であること。授業向けに作った保護期間中の曲を混ぜない(混ぜる場合は公開しない場所に分ける)
- [ ] 各曲の出典を README に明記
- [ ] SoundFont のライセンス条件と帰属表示
- [ ] 連携先の iPad アプリ および開発元との関係を README に明記する(非公式の周辺ツールであること、公式との混同を避ける)
- [ ] iPad Safari 実機で音が鳴ること、ロールが表示されること、スイッチで押し続けて音が伸びること
