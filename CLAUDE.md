# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

（プロジェクトの文書・UI・コメントは日本語で統一されている。新規に書くものも日本語に合わせること）

## 呼び方の決まり

**連携先の iPad アプリの製品名は、コード・コメント・UI・ドキュメント・コミットメッセージのどこにも書かない。** 決定事項なので、過去の文章を引用するときも書き換える。

| 指すもの | 使う言葉 |
|---|---|
| JSON のイベント配列 | タップ演奏データ |
| 連携先の iPad アプリ | 連携先の iPad アプリ / iPad アプリ |
| 開発元 | 開発元 |

## このリポジトリの状態

ビルドツール・パッケージマネージャ・テストランナーは一切ない。素の HTML/CSS/JS を GitHub Pages で配信する前提。

```
index.html            本体。CSS・JS・全曲データを内包した単一ファイル
sf/*.js               音色データ(生成物)。sf/LICENSE に帰属表示
tools/build-sf.py     sf/*.js を作り直すスクリプト
docs/license-nc891.png ラデツキー行進曲の素材ライセンスの控え
dt-player-仕様書.md    作業の一次情報源
```

`dt-player-仕様書.md` が**一次情報源**。背景・データ形式の制約・実機で直した箇所の理由・作業段階・守るべき設計判断が書いてある。実装に入る前に必ず読む。

## 動作確認

```sh
open index.html                  # file:// でも動く(SoundFont だけは合成音に落ちる)
python3 -m http.server 8000      # sf/ を読ませるにはこちら
python3 tools/build-sf.py        # sf/*.js を作り直す(通常は不要)
```

公開先は https://yamaguchitoshi.github.io/dt-player/ (main ブランチのルート)。

自動テストはない。検証は iPad Safari 実機で「音が鳴る / ピアノロールが出る / 押し続けて音が伸びる」を確認する。仕様書 6 章の各段階ごとに実機確認する運用。

## アーキテクチャ

単一 HTML の中は5つの層に分かれており、仕様書 5 章はこの境界でファイル分割する計画になっている。

- **音源** `Synth` / `SF` の2実装 + `audio()` / `noteOn()` / `allOff()` → `js/synth.js`
  下の「音源の2実装」を参照。ノートごとに gain を作り `live[]` に `{g, nodes}` で積む。`allOff()` が `live[]` を全部リリースして空にする
- **状態** `song` / `idx` / `scroll` / `lo,hi` / `held` → `js/app.js`
- **演奏** `press()` / `release()` / `step()` → `js/player.js`
- **描画** `fit()` / `draw()` / `paint()` → `js/roll.js`
- **読み込み** ファイル選択ハンドラ → `js/loader.js`（曲データは `data/songs.json` + `data/songs/*.json` へ外出し）

### 押さえておく不変条件

- `idx` は**次に鳴らすイベント**を指す。いま鳴っている音は `idx-1`（`paint()` の `cur`）
- `press()` の順序は「`allOff()` → 発音 → `idx++`」。同じ音の連続打鍵が頻出するので、**発音前に前の音を止めるのを省略してはいけない**
- `release()` は減衰音色（`d>0`、ピアノ）では何もしない。持続音色だけが離鍵で止まる
- `scroll` は `idx` へ補間で追従する値（`prefers-reduced-motion` 時は即座に代入）。`idx` とは別物
- `held` フラグと `ev.repeat` 無視でキーリピートによる多重進行を防ぐ

### 画面の構え

演奏中に見えているのは譜面だけでよい、という前提で組んである。

```
.top       曲名・カウンタ・「曲と音色」ボタン   54px
#stagewrap 譜面(canvas)                        残り全部
.read      次の音・1つ戻す・最初から           76px
.panel     曲と音色(引き出し)                  position:fixed で重ねる
```

**引き出しは `position:fixed` で上に重ねる。グリッドを組み替えない。** 組み替えると canvas が再測定され、Safari の高さ0問題を踏む余地ができる。重ねるだけなら canvas のサイズは一切動かない。

`.read` の音名はこの画面の主役。色の判別が難しい人への手がかりという仕様書 8 章の要件を、隅の小さな文字ではなく読み取り欄として扱っている。等幅で組み、音ごとに `<span>` に分けて間隔を CSS で決める(連続スペースは詰められるため)。

**引き出しを開いたら `.panel` そのものにフォーカスを移す。曲や音色のボタンには乗せない。** ボタンに乗せると、支援者が設定を開いている間に本人がスイッチ(=スペース)を押したとき、そのボタンが実行されてしまう。曲ボタンなら演奏位置が 0 に戻る。`panelOpen` の間は `press()` にも入力を渡さない。

曲を選んだら閉じる(決めの操作)。音色を替えても開いたまま(聴き比べる操作)。

### 名前

**`dt-player`。** 連携先アプリの製品名を冠した名前は使わない。公式アプリそのものの名前に見え、仕様書 10 章の「開発元との混同を避ける」に反するため。引き出しの見出しに非公式である旨を書いてある。消さないこと。

### 音源の2実装

`Synth`(合成音)と `SF`(SoundFont)が同じ約束を実装している。**この約束を崩さないこと。**

| メンバ | 意味 |
|---|---|
| `noteOn(midi, vel)` | 1音鳴らして `live[]` に積む |
| `decays(音色)` | 押しっぱなしでも減衰する音色か。`release()` の分岐に使う |
| `rel(音色)` | 離してから消えるまでの秒数。`allOff()` が使う |

**鳴らすのは SoundFont だけ。** 合成音は利用者に見える選択肢ではなく、読み込みが終わるまでの代役と、失敗したときの受け皿として残してある。`engine` は `pickEngine()` が `SF.ready(tone)` だけで決める。UI に音源の切替はない。

これが**フォールバックの本体**。読み込みに失敗しても無音にはせず、合成音で鳴らし続けながら理由を `#sfmsg` に出す(仕様書 6 章「音が出ないより音質が落ちるほうが良い」)。うまくいっているときは `#sfmsg` を空にして黙る。

`SF` は `sf/<音色>.js` を `<script>` で読み込む(`fetch` ではないので `file://` でも CORS に当たらない)。**ページを開いた時点で先読みを始める。** 復号には AudioContext が要るが、それを待つと最初の一音までタップを待たせることになるので、`SF.decodeCtx()` が AudioContext がまだ無い間は `OfflineAudioContext` を使う(操作を待たずに作れる)。中身は base64 の MP3 で、`decodeAudioData` に通してから `AudioBufferSourceNode` の `loop` / `loopStart` / `loopEnd` で伸ばす。**ループ情報があることが SoundFont を採用した理由**なので、ループを切る変更をしてはいけない。減衰音色(ピアノ)はループさせたまま包絡線のほうを落とす。サンプルが尽きて不自然に切れるのを避けるため。

### 弾けるエフェクト

音が出たことを目でも確かめられるようにするもの(`spark()` / `drawSparks()`)。鳴った瞬間にブロックが白く弾け、十字のきらめきが散って消える。スイッチを押した手応えを聴覚以外でも返すのが目的。

**この効果には安全上の配慮が入っている。理由を理解せずに派手にしないこと。**

- 明滅させるのは小さなブロックの周りだけで、画面全体は光らせない。利用者に発作の既往がある場合を考え、広い面積が短時間に明暗するのを避けている
- 粒は `MAXBIT` (120) で頭打ちにしてある。速く連打されたときに積み上がって明滅面積が広がるのを防ぐため。描画負荷の対策でもある
- `prefers-reduced-motion` のときは粒を飛ばさず、その場で淡く光って消えるだけにする(`pops` の `calm`)

左端の鍵盤は**白鍵を土台に敷いてから黒鍵を左寄りに重ねる**。半音ごとの行に白鍵を1つずつ描くと白が黒鍵行で分断され、鍵盤に見えなくなる。

描画は `fillRect` だけで、`roundRect` は使わない(古い iPadOS 対策)。位置は画面座標なので、譜面がスクロールしても粒は追従しない。寿命が短いので気にならない。

### sf/ の中身は生成物

`sf/*.js` は `tools/build-sf.py` の出力。**手で編集しない。** WebAudioFont の音色データ(MIT)から MIDI 48〜84 のゾーンだけを抜き、3半音ごとに間引いてある。中間の音は `playbackRate` で補う。取得元はスクリプト内でコミットに固定してある。

ライセンスは `sf/LICENSE` にまとめてある。要点は、FluidR3(MIT, Frank Wen)が**派生音源に著作権表示を含めることを求めている**こと。`sf/*.js` の先頭にも同じ表示が入っているので消さない。WebAudioFont の再生エンジンは GPL-3.0 なので**取り込んでいない**。再生は `SF` が自前でやっている。

### タップ演奏データの形式

`[{"values":[72,64,48],"velocity":100}, ...]` の配列。1要素が1タップ。**時間情報を一切持たない**（発音タイミング・音長・ノートオフ・テンポ・休符は操作者の入力が決める）。これは欠落ではなく仕様なので、時間軸を持ち込む変更をしてはいけない。音域は MIDI 48〜84。範囲外は落とさず、鳴らさずに警告する。

## 触ってはいけない箇所（iPadOS Safari 実機で直したもの）

理由は仕様書 3 章に列挙されている。「冗長に見える」という理由で削らない。

- **音声解錠** — 初回タップで AudioContext 生成 + 1サンプル無音再生、加えて `<audio id="keepalive">` の無音 WAV を volume 0.001 でループ（消音状態でも鳴らすため）
- **canvas のサイズ取得** — `height:100%` は Safari のグリッド行内で 0 になる。`#stagewrap` を `position:relative` にして canvas を absolute で広げ、サイズは `getBoundingClientRect()` から取る。グリッド行は `minmax(0,1fr)`。40px 未満なら rAF で測り直す
- **`roundRect` を使わない** — iPadOS 16.3 以前に未実装。`fillRect` で描く
- **`??` 演算子を使わない** — 古い Safari 対策で置換済み
- **`draw()` の try/catch** — 描画例外で白画面にせず、画面にエラー文を出す
- **ジェスチャ抑制** — `dblclick` / `contextmenu` / `gesturestart` の preventDefault、`touch-action:none`
- **入力判定は `ev.code` と `ev.key` の両方** — `ev.code` が空になる Bluetooth スイッチ対策
- **ボタンを押したあと譜面に焦点を戻す処理** — Chrome はボタンをクリックするとフォーカスが残る。残ったままだと次のスペース入力がボタンの再実行に吸われ、演奏が進まない。支援者がマウスで「1つ戻す」を押し、本人がスイッチで続ける場面で壊れる。`ev.detail` が 0 のとき(キーボードからの起動)は触らないので、タブ移動は壊れない

## 設計上の前提（機能ではなく制約）

利用者にスイッチ操作をする重度身体障害の人が含まれる。仕様書 8 章の内容で、変更提案の可否を判断する基準になる。

- 押している間だけ音が伸びる挙動を変えない（タップで自動減衰にすると表現の核が失われる）
- 色だけで情報を伝えない。次の音は必ずテキストでも出す（`#hint`）
- キーボードだけで全機能に到達できる状態を保つ。フォーカス表示を消さない
- 「1つ戻す」は必須。押し間違いから復帰できること
- `prefers-reduced-motion` を尊重する
- エラー時に無言で止まらない。音・データ・描画のどれが失敗しても画面に理由を出す

## 収録データの扱い

7曲すべて保護期間終了の楽曲。出典と照合方法は仕様書 4 章にあり、README に明記する必要がある。保護期間中の曲を混ぜない。

ラデツキー行進曲だけは楽曲の保護期間とは別に**素材側の許諾が絡む**。ニコニ・コモンズの MIDI 素材 nc891（投稿者 nano_001）から抽出したもの。ライセンス条件は仕様書 4 章の表と `docs/license-nc891.png` にある。要点は2つ。

- **利用許可範囲「インターネット全般」／権利表記「任意」** — GitHub Pages での無償公開は許諾範囲に入り、クレジット表記は義務ではない
- **営利利用は個人でも「要連絡」** — 有償の教材に組み込む、企業として配布するなどの段階に進むときは、事前に nano_001 氏への連絡が要る。**この一線を越える変更を提案する前に必ず確認すること**

配布するのは MIDI そのものではなく抽出した音高データ（子作品にあたる）であって素材の再配布ではない、という区別を崩さない。

連携先の iPad アプリは開発元が開発するもので、本プロジェクトは**非公式の周辺ツール**。公式との混同を避ける記述を README に入れる。その際もアプリの製品名は書かない。
